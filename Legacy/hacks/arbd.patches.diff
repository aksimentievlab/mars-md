Only in arbd.patches/src: add_docs.py
Only in /home/pinyili2/server10/arbd_stable/src: Angle.cu
Only in /home/pinyili2/server10/arbd_stable/src: Angle.h
diff arbd.patches/src/arbd.cpp /home/pinyili2/server10/arbd_stable/src/arbd.cpp
1,8c1,2
< #ifdef USE_MPI
< #include <mpi.h>
< #endif
< 
< #ifdef USE_CUDA
< #include <cuda.h>
< #endif
< 
---
> ///////////////////////////////////////////////////////////////////////
> // Author: Jeff Comer <jcomer2@illinois.edu>
9a4
> #include <cuda.h>
12c7,9
< // #include "useful.h"
---
> #include "useful.h"
> #include "GrandBrownTown.h"
> #include "Configuration.h"
14d10
< #include "SignalManager.h"
16c12
< #include "SimManager.h"
---
> #include "SignalManager.h"
24,48d19
< 
< #ifdef USE_MPI
<     MPI_Init(NULL, NULL);
< 
<     // Get the number of processes
<     int world_size;
<     MPI_Comm_size(MPI_COMM_WORLD, &world_size);
< 
<     // Get the rank of the process
<     int world_rank;
<     MPI_Comm_rank(MPI_COMM_WORLD, &world_rank);
< 
<     // Get the name of the processor
<     char processor_name[MPI_MAX_PROCESSOR_NAME];
<     int name_len;
<     MPI_Get_processor_name(processor_name, &name_len);
< 
<     // Print off a hello world message
<     printf("Hello world from processor %s, rank %d out of %d processors\n",
<            processor_name, world_rank, world_size);
< 
<     // Finalize the MPI environment.
<     MPI_Finalize();
< #endif
< 
71c42
< 	    printf("%s unknown version\n", argv[0]);
---
> 	    printf("%s Nov 2016 (alpha)\n", argv[0]);
75,81c46,50
< #ifdef USE_CUDA
< 	    GPUManager::load_info();
< #else
< 	    printf("ARBD not compiled with CUDA support\n");
< #endif
< 	    printf("Returning\n");
< 	    return 0;
---
> 		// --info
> 		GPUManager::load_info();
> 		printf("Returning\n");
> 		// size_t n_gpus = max(GPUManager::gpus.size(), 1lu);
> 		return 0;
87,90c56,59
< 	printf("--- Atomic Resolution Brownian Dynamics ---\n");
< 
< 	size_t n_gpus = 0;
< #ifdef USE_CUDA
---
> 	// printf("Everything's great when you're...BrownTown\n");
> 	printf("  –––––––––––––––––––––––––––––––––––––––––––––\n");
> 	printf("  |    Atomic Resolution Brownian Dynamics    |\n");
> 	printf("  –––––––––––––––––––––––––––––––––––––––––––––\n");
92c61,62
< 	n_gpus = GPUManager::allGpuSize();
---
> 
> 	size_t n_gpus = GPUManager::allGpuSize();
94d63
< #endif
114,115d82
< #ifdef USE_CUDA
< 		    /*
126,132c93,99
< 			// std::vector<unsigned int>::iterator it;
< 			// it = std::find(gpuIDs.begin(), gpuIDs.end(), arg_val);
< 			// if (it != gpuIDs.end()) {
< 			//     printf("WARNING: ignoring repeated GPU ID %d\n", arg_val);
< 			// } else {
< 			gpuIDs.push_back(arg_val);
< 			// }
---
> 			std::vector<unsigned int>::iterator it;
> 			it = std::find(gpuIDs.begin(), gpuIDs.end(), arg_val);
> 			if (it != gpuIDs.end()) {
> 			    printf("WARNING: ignoring repeated GPU ID %d\n", arg_val);
> 			} else {
> 			    gpuIDs.push_back(arg_val);
> 			}
137,141c104
< 		    */
< #else
< 		    printf("ARBD compiled without CUDA support. Exiting.\n");
< 		    return 1;
< #endif			
---
> 			
180d142
< #ifdef USE_CUDA
187c149
< 	    Exception(ValueError, "Using more than one GPU requires compilation with USE_NCCL flag");
---
> 	    printf("ERROR: more than one GPU requires compilation with USE_NCCL flag\n");
193d154
< #endif
195,203c156,163
< 	SimManager sim;
< 	sim.run();
< 	
< 	// Configuration config(configFile, replicas, debug);
< 	// config.copyToCUDA();
< 	// // GPUManager::set(0);
< 	// GrandBrownTown brown(config, outArg,
< 	// 		debug, imd_on, imd_port, replicas);
< 	// brown.run();
---
> 	Configuration config(configFile, replicas, debug);
> 	config.copyToCUDA();
> 	// GPUManager::set(0);
> 
> 	GrandBrownTown brown(config, outArg,
> 			debug, imd_on, imd_port, replicas);
> 
> 	brown.run();
Only in arbd.patches/src: ARBDException.cpp
Only in arbd.patches/src: ARBDException.h
Only in arbd.patches/src: .backup
Only in /home/pinyili2/server10/arbd_stable/src: BaseGrid.cu
Only in /home/pinyili2/server10/arbd_stable/src: BaseGrid.h
Only in /home/pinyili2/server10/arbd_stable/src: BrownianParticle.cu
Only in /home/pinyili2/server10/arbd_stable/src: BrownianParticle.h
Only in /home/pinyili2/server10/arbd_stable/src: BrownianParticleType.cpp
Only in /home/pinyili2/server10/arbd_stable/src: BrownianParticleType.h
Only in /home/pinyili2/server10/arbd_stable/src: BrownParticlesKernel.h
Only in /home/pinyili2/server10/arbd_stable/src: CellDecomposition.cu
Only in /home/pinyili2/server10/arbd_stable/src: CellDecomposition.h
Only in arbd.patches/src: CMakeLists.txt
Only in arbd.patches/src: common.h
Only in /home/pinyili2/server10/arbd_stable/src: ComputeForce.cu
Only in /home/pinyili2/server10/arbd_stable/src: ComputeForce.cuh
Only in /home/pinyili2/server10/arbd_stable/src: ComputeForce.h
Only in /home/pinyili2/server10/arbd_stable/src: ComputeGridGrid.cu
Only in /home/pinyili2/server10/arbd_stable/src: ComputeGridGrid.cuh
Only in /home/pinyili2/server10/arbd_stable/src: ComputeJustForce.h
Only in /home/pinyili2/server10/arbd_stable/src: Configuration.cpp
Only in /home/pinyili2/server10/arbd_stable/src: Configuration.h
Only in /home/pinyili2/server10/arbd_stable/src: cuda-test.c
Only in /home/pinyili2/server10/arbd_stable/src: CudaUtil.cu
Only in /home/pinyili2/server10/arbd_stable/src: CudaUtil.cuh
Only in /home/pinyili2/server10/arbd_stable/src: DcdWriter.h
Only in /home/pinyili2/server10/arbd_stable/src: Debug.h
Only in /home/pinyili2/server10/arbd_stable/src: Dihedral.cu
Only in /home/pinyili2/server10/arbd_stable/src: Dihedral.h
Only in /home/pinyili2/server10/arbd_stable/src: Exclude.cu
Only in /home/pinyili2/server10/arbd_stable/src: Exclude.h
Only in arbd.patches/src: extras
Only in /home/pinyili2/server10/arbd_stable/src: findcudalib.mk
Only in /home/pinyili2/server10/arbd_stable/src: FlowForce.cpp
Only in /home/pinyili2/server10/arbd_stable/src: FlowForce.h
Only in /home/pinyili2/server10/arbd_stable/src: GPUController.h
diff arbd.patches/src/GPUManager.cpp /home/pinyili2/server10/arbd_stable/src/GPUManager.cpp
2d1
< #ifdef USE_CUDA
21a21,23
> #ifndef CUDART_VERSION
> #error CUDART_VERSION Undefined!
> #elif (CUDART_VERSION < 12000)
23c25,28
<     char* timeout_str = (char*)"";
---
> #else
>     cudaGetDeviceProperties_v2(&properties, id);
> #endif
>     printf("[%d] %s ", id, properties.name);
25c30
< 	timeout_str =  (char*)"(may timeout) ";
---
> 	printf("(may timeout) ");
30,33c35,38
<     LOGINFO("[{}] {} {}| SM {}.{} {:.2f}GHz, {:.1f}GB RAM",
< 	 id, properties.name, timeout_str, properties.major, properties.minor,
< 	 (float) properties.clockRate * 10E-7, (float) properties.totalGlobalMem * 7.45058e-10);
<     
---
>     printf("| SM %d.%d, ", properties.major, properties.minor);
>     printf("%.2fGHz, ", (float) properties.clockRate * 10E-7);
>     printf("%.1fGB RAM\n", (float) properties.totalGlobalMem * 7.45058e-10);
> 
62c67
<     LOGTRACE("Destroying streams");
---
>     // printf("Destroying streams\n");
67c72
< 		LOGTRACE("  destroying stream {} at {}\n", i, fmt::ptr((void *) &streams[i]));
---
> 		// printf("  destroying stream %d at %p\n", i, (void *) &streams[i]);
79c84
<     LOGINFO("Found {} GPU(s)", nGPUs);
---
>     printf("Found %d GPU(s)\n", nGPUs);
87c92
< 	Exception(ValueError, "Did not find a GPU\n");
---
> 	fprintf(stderr, "Error: Did not find a GPU\n");
99,100c104
<     LOGINFO("Initializing GPU devices... ");
<     char msg[256] = "";    
---
>     printf("Initializing devices... ");
103c107
<     	    sprintf(msg, "%s%d, ", msg, gpus[i].id);
---
>     	    printf("%d, ", gpus[i].id);
105c109
< 	    sprintf(msg, "%sand %d", msg, gpus[i].id);
---
>     	    printf("and %d\n", gpus[i].id);
107c111,112
<     	    sprintf(msg, "%d", gpus[i].id);
---
>     	    printf("%d\n", gpus[i].id);
> 
112d116
<     LOGINFO("Initializing GPUs: {}", msg);
188,189d191
< #endif
< 
diff arbd.patches/src/GPUManager.h /home/pinyili2/server10/arbd_stable/src/GPUManager.h
1c1,2
< #pragma once
---
> #ifndef GPU_MANAGER_H
> #define GPU_MANAGER_H
3,60d3
< #include "ARBDException.h"
< 
< #ifdef __CUDACC__
<     #define HOST __host__
<     #define DEVICE __device__
< #else
<     #define HOST
<     #define DEVICE
< #endif
< 
< #if defined(__CUDACC__) // NVCC
<    #define MY_ALIGN(n) __align__(n)
< #elif defined(__GNUC__) // GCC
<   #define MY_ALIGN(n) __attribute__((aligned(n)))
< #elif defined(_MSC_VER) // MSVC
<   #define MY_ALIGN(n) __declspec(align(n))
< #else
<   #error "Please provide a definition for MY_ALIGN macro for your host compiler!"
< #endif
< 
< #ifndef USE_CUDA
< struct MY_ALIGN(16) float4 {
<     float4() : x(0), y(0), z(0), w(0) {};
<     float4(float x, float y, float z, float w) : x(x), y(y), z(z), w(w) {};
<     float4 operator+(const float4&& o) {
< 	return float4(x+o.x,y+o.y,z+o.z,w+o.w);
<     };
<     float4 operator*(const float&& s) {
< 	return float4(x*s,y*s,z*s,w*s);
<     };
<     
<     float x,y,z,w;
< };
< #endif
< 
< // START traits
< // https://stackoverflow.com/questions/55191505/c-compile-time-check-if-method-exists-in-template-type
< #include <type_traits>
< // template<class ...Ts>
< // struct voider{
< //     using type = void;
< // };
< 
< // template<class T, class = void>
< // struct has_copy_to_cuda : std::false_type{};
< 
< // template<class T>
< // struct has_copy_to_cuda<T, typename voider<decltype(std::declval<T>().copy_to_cuda())>::type> : std::true_type{};
< 
< template <typename T, typename = void>
< struct has_copy_to_cuda : std::false_type {};
< 
< template <typename T>
< struct has_copy_to_cuda<T, decltype(std::declval<T>().copy_to_cuda(), void())> : std::true_type {};
< // END traits
< 
< 
< #ifdef USE_CUDA
65d7
< #include <cuda_runtime.h>
67c9
< // #include "useful.h"
---
> #include "useful.h"
83,88c25
< #define gpuErrchk(code) { if ((code) != cudaSuccess) {    \
< 	    Exception(CUDARuntimeError, " ", cudaGetErrorString(code)); \
< 	}}
< #endif
< /*
< define gpuErrchk(code) { if ((code) != cudaSuccess) {					       \
---
> #define gpuErrchk(code) { if ((code) != cudaSuccess) {					       \
91,92c28,29
< endif
< */
---
> #endif
> 
Only in /home/pinyili2/server10/arbd_stable/src: GrandBrownTown.cu
Only in /home/pinyili2/server10/arbd_stable/src: GrandBrownTown.cuh
Only in /home/pinyili2/server10/arbd_stable/src: GrandBrownTown.h
Only in /home/pinyili2/server10/arbd_stable/src: gridSampleDef.c
Only in /home/pinyili2/server10/arbd_stable/src: gridSampleFlow.c
Only in arbd.patches/src: headers.b64
Only in arbd.patches/src: headers.txt
Only in /home/pinyili2/server10/arbd_stable/src: imd.cpp
Only in /home/pinyili2/server10/arbd_stable/src: imd.h
Only in arbd.patches/src: Integrator
Only in arbd.patches/src: Integrator.cpp
Only in arbd.patches/src: Integrator.h
Only in arbd.patches/src: Interaction
Only in arbd.patches/src: Interaction.cpp
Only in arbd.patches/src: Interaction.h
Only in arbd.patches/src: intro_to_cpp.cpp
Only in /home/pinyili2/server10/arbd_stable/src: JamesBond.cu
Only in /home/pinyili2/server10/arbd_stable/src: JamesBond.h
Only in /home/pinyili2/server10/arbd_stable/src: makefile.clang
Only in /home/pinyili2/server10/arbd_stable/src: namd_common.h
Only in /home/pinyili2/server10/arbd_stable/src: nvtx_defs.h
Only in arbd.patches/src: old
Only in arbd.patches/src: oldcode
Only in /home/pinyili2/server10/arbd_stable/src: OverlordGrid.h
Only in arbd.patches/src: ParticlePatch
Only in arbd.patches/src: ParticlePatch.cpp
Only in arbd.patches/src: ParticlePatch.h
Only in arbd.patches/src: Patch.cpp
Only in arbd.patches/src: Patch.h
Only in arbd.patches/src: PatchOp.cu
Only in arbd.patches/src: PatchOp.cuh
Only in arbd.patches/src: PatchOp.h
Only in /home/pinyili2/server10/arbd_stable/src: ProductPotential.h
Only in arbd.patches/src: Proxy.h
Only in arbd.patches/src: pybindings
Only in /home/pinyili2/server10/arbd_stable/src: RandomCPU.h
Only in arbd.patches/src: Random.cu
Only in arbd.patches/src: Random.cu.bak
Only in /home/pinyili2/server10/arbd_stable/src: RandomCUDA.cu
Only in /home/pinyili2/server10/arbd_stable/src: RandomCUDA.h
diff arbd.patches/src/Random.h /home/pinyili2/server10/arbd_stable/src/Random.h
1,333c1,29
< #pragma once
< 
< // TODO: clean comments and probably rework interface
< 
< #include "GPUManager.h"
< #include "Types.h"
< #include "Proxy.h"
< #include <map>
< 
< #include <cuda.h>
< #include <cuda_runtime.h>
< #include <curand.h>
< #include <curand_kernel.h>
< 
< // #ifdef __CUDA_ARCH__
< // #include <curand_kernel.h>
< // #else
< // #define QUALIFIERS __host__ __device__
< // #define __device_builtin_variable_blockDim 1
< // #define __device_builtin_variable_threadIdx 0
< // #include <curand_kernel.h>
< // #undef QUALIFIERS
< // #endif
< 
< 
< 
< #define cuRandChk(ans) { cuRandAssert((ans), __FILE__, __LINE__); }
< inline void cuRandAssert(curandStatus code, const char *file, int line, bool abort=true) {
< 	if (code != CURAND_STATUS_SUCCESS) {
< 		fprintf(stderr, "CURAND Error: %d (%s:%d)\n", code, file, line);
< 		if (abort) exit(code);
< 	}
< }
< 
< namespace Random {
<     // Interface
<     template<typename RNG>
<     HOST DEVICE inline typename RNG::state_t* get_gaussian_state(RNG *random) {
< 	// return nullptr;
< 	return random->get_gaussian_state();
<     }
< 
<     template<typename RNG>
<     HOST DEVICE void set_gaussian_state(RNG *random, typename RNG::state_t* state) {
< 	return random->set_gaussian_state(state);
<     }
< 
<     // template<typename RNG>
<     // HOST DEVICE auto gaussian(RNG __restrict__ * random, typename RNG::state_t __restrict__ * state) {
<     // 	return random->gaussian(state);
<     // }
< 
<     template<typename RNG>
<     HOST DEVICE float gaussian(RNG *random, typename RNG::state_t* state) {
< 	return random->gaussian(state);
<     }
< }
< 
< // class RandomCPU {
< // public:
< //     using state_t = void;
< 
< //     // Methods for manipulating the state
< //     void init(size_t num, unsigned long seed, size_t offset) {
< // 	// 
< //     };
< 
< //     // Methods for generating random values
< //     HOST DEVICE inline float gaussian(state_t* state) {
< // 	LOGWARN("RandomCPU::gaussian(): not implemented");
< // 	return 0;
< //     };
< 
< //     HOST DEVICE inline state_t* get_gaussian_state() { return nullptr; };
< 
< //     HOST DEVICE inline void set_gaussian_state(state_t* state) {};
< 
< //     // // HOST DEVICE inline float gaussian(RandomState* state) {};
< //     // HOST inline Vector3 gaussian_vector(size_t idx, size_t thread) {
< //     // 	return Vector3();
< //     // };
< //     // unsigned int integer() {
< //     // 	return 0;
< //     // };
< //     // unsigned int poisson(float lambda) {
< //     // 	return 0;
< //     // };
< //     // float uniform() {
< //     // 	return 0;
< //     // };
< //     // void reorder(int *a, int n);
< // };
< 
< // Using host cuRAND
< template<size_t num_states = 128>
< class RandomCPU {
<     static_assert( num_states > 0 );
<     static_assert( (num_states/2)*2 == num_states ); // cuRand pseudo-RNG must generate even samples 
< public:
<     using state_t = curandStateXORWOW_t;
< 
<     HOST RandomCPU() {
< 	LOGINFO("Creating RandomCPU");
< 	// assert( location.type == Resource::GPU );
< 	cuRandChk( curandCreateGeneratorHost(&generator, CURAND_RNG_PSEUDO_XORWOW) );
< 	// states = new state_t[num_states]; // TODO: This may not be needed, or even compatible with HOST API
< 	buffer_idx = num_states;
<     }
< 
<     // HOST ~RandomCPU() {
<     // 	LOGINFO("Destroying RandomCPU");
<     // 	// if (states != nullptr) delete [] states;
<     // 	// states = nullptr;
<     // }
< 
<     // Methods for manipulating the state
<     void init(unsigned long seed) {
< 	curandSetPseudoRandomGeneratorSeed( generator, seed );
<     };
< 
<     // Methods for generating random values
<     HOST inline float gaussian(state_t* state) {
< 	if (buffer_idx == num_states) {
< 	    fill_buffer();
< 	}
< 	return buffer[buffer_idx++];
<     };
< 
<     HOST inline state_t* get_gaussian_state() { return nullptr; };
< 
<     HOST inline void set_gaussian_state(state_t* state) {};
<     
<     // // HOST DEVICE inline float gaussian(RandomState* state) {};
<     // HOST inline Vector3 gaussian_vector(size_t idx, size_t thread) {
<     // 	return Vector3();
<     // };
<     // unsigned int integer() {
<     // 	return 0;
<     // };
<     // unsigned int poisson(float lambda) {
<     // 	return 0;
<     // };
<     // float uniform() {
<     // 	return 0;
<     // };
<     // void reorder(int *a, int n);
< 
<     static void test(size_t num_vals = 4*num_states, unsigned long seed = 1, size_t offset = 0, RandomCPU<num_states>* rng = nullptr) {
< 	using RNG = RandomCPU<num_states>;
<     
< 	bool rng_was_null = (rng == nullptr);
< 	if (rng_was_null) {
< 	    rng = new RNG{};
< 	    // RNG::instantiate_on_GPU();
< 	}
< 	rng->init(seed);
< 
< 	float *buf = new float[num_vals];
< 
< 	LOGTRACE("...running test");
< 	for (size_t i = 0; i < num_vals; ++i) {
< 	    buf[i] = Random::gaussian<RNG>(rng, rng->states);
< 	}
< 	// for (size_t i = 0; i < num_vals; ++i) {
< 	//     size_t num = 1;
< 	//     float mean = 0.0f;
< 	//     float stddev = 1.0f;
< 	//     float val;
< 	//     cuRandChk( curandGenerateNormal( rng->generator, &val, num, mean, stddev  ));
< 	//     // buf[i] = Random::gaussian<RNG>(rng, rng->states);
< 	//     buf[i] = val;
< 	//     if ((i%64) == 0) LOGINFO("...running test {}", buf[i]);
< 	// }
< 
< 	// LOGTRACE("...running test");
< 	// for (size_t i = 0; i < num_vals; ++i) {
< 	//     size_t num = 2;
< 	//     float mean = 0.0f;
< 	//     float stddev = 1.0f;
< 	//     float val;
< 	//     float *buf2 = new float[num];
< 	//     curandGenerateNormal( rng->generator, buf2, num, mean, stddev );
< 	//     // buf[i] = Random::gaussian<RNG>(rng, rng->states);
< 	//     buf[i] = buf2[1];
< 	//     if ((i%64) == 0) LOGINFO("...running test {}", buf[i]);
< 	// }
< 
< 	
< 	// curandGenerateNormal( rng->generator, buf, num_vals, 0.0f, 1.0f );
< 	
< 	float sum = 0;
< 	float sum2 = 0;
< 	for (size_t i = 0; i < num_vals; ++i) {
< 	    //if (((i+1)%64) < 2) LOGINFO("...running test: buf[{}] == {}", i, buf[i]);
< 	    LOGINFO("...running test: buf[{}] == {}", i, buf[i]);
< 	    sum += buf[i];
< 	    sum2 += buf[i]*buf[i];
< 	}
< 
< 	// curandGenerateNormal( rng->generator, buf, num_vals, 0.0f, 1.0f );
< 	
< 	// sum = 0;
< 	// sum2 = 0;
< 	// for (size_t i = 0; i < num_vals; ++i) {
< 	//     if ((i%64) == 0) LOGINFO("...running test {}", buf[i]);
< 	//     sum += buf[i];
< 	//     sum2 += buf[i]*buf[i];
< 	// }
< 	
< 	LOGINFO("RNG gaussian val0, val1, mean, std, count: {}, {}, {}, {}", buf[0], buf[1], sum/num_vals, sqrt((sum2/num_vals) - sum*sum/(num_vals*num_vals)), num_vals);
< 	// LOGINFO("RNG gaussian mean, std, count: {}, {}, {}", sum/num_vals, sqrt((sum2/num_vals) - sum*sum/(num_vals*num_vals)), num_vals);
< 
< 	if (rng_was_null) delete rng;
< 	delete[] buf;
<     };
< 
< 
< 
<     
< private:    
<     HOST void fill_buffer() {
< 	const float mean = 0.0f;
< 	const float stddev = 1.0f;
< 	cuRandChk( curandGenerateNormal( generator, buffer, num_states, mean, stddev ) );
< 	buffer_idx = 0;
<     };
< 
<     curandGenerator_t generator;
<     state_t *states;		// TODO remove??? (for now here to satisfy interface)
<     float buffer[num_states];	// TODO use another template paramter for number of states?
<     size_t buffer_idx;
< };
< 
< // template<size_t num_states = 128, size_t chunk_size = 128>
< // class RandomGPUMimic {
< //     static_assert( num_states > 0 );
< //     static_assert( num_states <= 4096);
< //     static_assert( chunk_size > 0 );
< //     static_assert( (chunk_size/2)*2 == chunk_size ); // cuRand pseudo-RNG must generate even samples 
< // public:
< //     using state_t = curandStateXORWOW_t;
< 
< //     HOST RandomGPUMimic() {
< // 	LOGINFO("Creating RandomGPUMimic");
< // 	// assert( location.type == Resource::GPU );
< // 	for (size_t i=0; i < num_states; ++i) {
< // 	    cuRandChk( curandCreateGeneratorHost(&(generator[i]), CURAND_RNG_PSEUDO_XORWOW) );
< // 	    buffer_idx[i] = chunk_size;
< // 	}
< // 	generator_idx = 0;	
< //     };
< 
< //     // Methods for manipulating the state
< //     void init(unsigned long seed, size_t offset = 0) {
< // 	LOGINFO("...init");
< // 	states = new state_t[num_states];
< // 	for (size_t i=0; i < num_states; ++i) {	
< // 	    // LOGINFO("...init {}", i, &(states[i]));
< // 	    ( curand_init(seed, i, offset, &(states[i])) );
< // 	}
< // 	LOGINFO("...init done");	
< //     };
< 
< //     // Methods for generating random values
< //     HOST inline float gaussian(state_t* state) {
< // 	if (generator_idx == num_states) {
< // 	    generator_idx = 0;
< // 	}
< // 	return curand_normal( &(states[generator_idx++]) );
< //     };
< 
< //     HOST inline state_t* get_gaussian_state() { return nullptr; };
< 
< //     HOST inline void set_gaussian_state(state_t* state) {};
<     
< //     static void test(size_t num_vals = 4*num_states, unsigned long seed = 1, size_t offset = 0, RandomGPUMimic<num_states,chunk_size>* rng = nullptr) {
< // 	using RNG = RandomGPUMimic<num_states, chunk_size>;
<     
< // 	bool rng_was_null = (rng == nullptr);
< // 	if (rng_was_null) {
< // 	    rng = new RNG{};
< // 	    // RNG::instantiate_on_GPU();
< // 	}
< // 	rng->init(seed);
< 
< // 	float *buf = new float[num_vals];
< 
< // 	LOGTRACE("...running test");
< // 	for (size_t i = 0; i < num_vals; ++i) {
< // 	    buf[i] = Random::gaussian<RNG>(rng, rng->states);
< // 	}
< 	
< // 	float sum = 0;
< // 	float sum2 = 0;
< // 	for (size_t i = 0; i < num_vals; ++i) {
< // 	    // if (((i+1)%64) < 2) LOGINFO("...running test: buf[{}] == {}", i, buf[i]);
< // 	    LOGINFO("...running test: buf[{}] == {}", i, buf[i]);
< // 	    sum += buf[i];
< // 	    sum2 += buf[i]*buf[i];
< // 	}
< 	
< // 	LOGINFO("RNG gaussian val0, val1, mean, std, count: {}, {}, {}, {}", buf[0], buf[1], sum/num_vals, sqrt((sum2/num_vals) - sum*sum/(num_vals*num_vals)), num_vals);
< // 	// LOGINFO("RNG gaussian mean, std, count: {}, {}, {}", sum/num_vals, sqrt((sum2/num_vals) - sum*sum/(num_vals*num_vals)), num_vals);
< 
< // 	if (rng_was_null) delete rng;
< // 	delete[] buf;
< //     };
<     
< // private:    
< //     HOST void fill_buffer(size_t gen_idx) {
< // 	const float mean = 0.0f;
< // 	const float stddev = 1.0f;
< // 	cuRandChk( curandGenerateNormal( generator[gen_idx], buffer[gen_idx], chunk_size, mean, stddev ) );
< // 	buffer_idx[gen_idx] = 0;
< //     };
< 
< //     curandGenerator_t generator[num_states];
< //     // state_t states[num_states];
< //     state_t* states;
< //     float buffer[num_states][chunk_size];	// TODO use another template paramter for number of states?
< //     size_t buffer_idx[num_states];
< //     size_t generator_idx;
<     
< // };
< 
< template<size_t num_states = 128, size_t chunk_size = 128>
< class RandomGPUMimicAttempt {
<     static_assert( num_states > 0 );
<     static_assert( num_states <= 4096);
<     static_assert( chunk_size > 0 );
<     static_assert( (chunk_size/2)*2 == chunk_size ); // cuRand pseudo-RNG must generate even samples 
< public:
<     using state_t = curandStateXORWOW_t;
---
> /**
> ***  Copyright (c) 1995, 1996, 1997, 1998, 1999, 2000 by
> ***  The Board of Trustees of the University of Illinois.
> ***  All rights reserved.
> **/
> 
> /*
>  * Copyright (c) 1993 Martin Birgmeier
>  * All rights reserved.
>  *
>  * You may redistribute unmodified or modified versions of this source
>  * code provided that the above copyright notice and this and the
>  * following conditions are retained.
>  *
>  * This software is provided ``as is'', and comes with no warranties
>  * of any kind. I shall in no event be liable for anything that happens
>  * to anyone/anything when using this software.
>  */
> 
> #ifndef RANDOM_H
> #define RANDOM_H
> 
> #ifdef __CUDACC__
>     #define HOST __host__
>     #define DEVICE __device__
> #else
>     #define HOST 
>     #define DEVICE 
> #endif
335,364c31,32
<     HOST RandomGPUMimicAttempt() {
< 	LOGINFO("Creating RandomGPUMimicAttempt");
< 	// assert( location.type == Resource::GPU );
< 	for (size_t i=0; i < num_states; ++i) {
< 	    cuRandChk( curandCreateGeneratorHost(&(generator[i]), CURAND_RNG_PSEUDO_XORWOW) );
< 	    buffer_idx[i] = chunk_size;
< 	}
< 	generator_idx = 0;
<     };
< 
<     // Methods for manipulating the state
<     void init(unsigned long seed) {
< 	for (size_t i=0; i < num_states; ++i) {	
< 	    cuRandChk( curandSetPseudoRandomGeneratorSeed( generator[i], seed ) );
< 	    cuRandChk( curandSetGeneratorOffset( generator[i], i ) );
< 	}
<     };
< 
<     // Methods for generating random values
<     HOST inline float gaussian(state_t* state) {
< 	if (generator_idx == num_states) {
< 	    generator_idx = 0;
< 	}
< 	if (buffer_idx[generator_idx] == chunk_size) {
< 	    fill_buffer(generator_idx);
< 	}
< 	float ret = buffer[generator_idx][buffer_idx[generator_idx]++];
< 	++generator_idx;
< 	return ret;
<     };
---
> #include "common.h"
> #include "useful.h"
366c34,38
<     HOST inline state_t* get_gaussian_state() { return nullptr; };
---
> #ifdef _MSC_VER
> #define INT64_LITERAL(X) X ## i64
> #else
> #define INT64_LITERAL(X) X ## LL
> #endif
368,417c40,43
<     HOST inline void set_gaussian_state(state_t* state) {};
<     
<     static void test(size_t num_vals = 4*num_states, unsigned long seed = 1, size_t offset = 0, RandomGPUMimicAttempt<num_states,chunk_size>* rng = nullptr) {
< 	using RNG = RandomGPUMimicAttempt<num_states, chunk_size>;
<     
< 	bool rng_was_null = (rng == nullptr);
< 	if (rng_was_null) {
< 	    rng = new RNG{};
< 	    // RNG::instantiate_on_GPU();
< 	}
< 	rng->init(seed);
< 
< 	float *buf = new float[num_vals];
< 
< 	LOGTRACE("...running test");
< 	for (size_t i = 0; i < num_vals; ++i) {
< 	    buf[i] = Random::gaussian<RNG>(rng, rng->states);
< 	}
< 	
< 	float sum = 0;
< 	float sum2 = 0;
< 	for (size_t i = 0; i < num_vals; ++i) {
< 	    // if (((i+1)%64) < 2) LOGINFO("...running test: buf[{}] == {}", i, buf[i]);
< 	    LOGINFO("...running test: buf[{}] == {}", i, buf[i]);
< 	    sum += buf[i];
< 	    sum2 += buf[i]*buf[i];
< 	}
< 	
< 	LOGINFO("RNG gaussian val0, val1, mean, std, count: {}, {}, {}, {}", buf[0], buf[1], sum/num_vals, sqrt((sum2/num_vals) - sum*sum/(num_vals*num_vals)), num_vals);
< 	// LOGINFO("RNG gaussian mean, std, count: {}, {}, {}", sum/num_vals, sqrt((sum2/num_vals) - sum*sum/(num_vals*num_vals)), num_vals);
< 
< 	if (rng_was_null) delete rng;
< 	delete[] buf;
<     };
<     
< private:    
<     HOST void fill_buffer(size_t gen_idx) {
< 	const float mean = 0.0f;
< 	const float stddev = 1.0f;
< 	cuRandChk( curandGenerateNormal( generator[gen_idx], buffer[gen_idx], chunk_size, mean, stddev ) );
< 	buffer_idx[gen_idx] = 0;
<     };
< 
<     curandGenerator_t generator[num_states];
<     state_t *states;		// TODO remove??? (for now here to satisfy interface)
<     float buffer[num_states][chunk_size];	// TODO use another template paramter for number of states?
<     size_t buffer_idx[num_states];
<     size_t generator_idx;
<     
< };
---
> #define	RAND48_SEED   INT64_LITERAL(0x00001234abcd330e)
> #define	RAND48_MULT   INT64_LITERAL(0x00000005deece66d)
> #define	RAND48_ADD    INT64_LITERAL(0x000000000000000b)
> #define RAND48_MASK   INT64_LITERAL(0x0000ffffffffffff)
418a45
> class Random {
419a47
> private:
421c49,53
< #ifdef USE_CUDA
---
>  float second_gaussian;
>   int64 second_gaussian_waiting;
>   int64 rand48_seed;
>   int64 rand48_mult;
>   int64 rand48_add;
423,430d54
< // Forward declarations
< template<size_t num_states> class RandomGPU;
< template<size_t num_states>
< __global__ void RandomGPU__test_kernel(RandomGPU<num_states>* rng, size_t num_vals, float* buf, unsigned long seed, size_t offset);
< 
< template<size_t num_states = 128>
< class RandomGPU {
<     static_assert(num_states > 0);
432,434d55
<     using state_t = curandStateXORWOW_t;
< 
<     // DEVICE RandomGPU(Random::Conf c, Resource& location) : num_states{c.num_threads}, states{nullptr} {
436,442c57,187
<     HOST DEVICE RandomGPU() : states{nullptr} {
< 	// Not sure how this works on host
< 	assert( threadIdx.x + blockIdx.x == 0 );
< 	
< 	LOGINFO("Creating RandomGPU");
< 	// assert( location.type == Resource::GPU );
< 	states = new state_t[num_states];
---
>   // default constructor
>   Random(void) {
>     init(0);
>     rand48_seed = RAND48_SEED;
>   }
> 
>   // constructor with seed
>   Random(unsigned long seed) {
>     init(seed);
>   }
> 
>   // reinitialize with seed
>   HOST DEVICE inline void init(unsigned long seed) {
>     second_gaussian = 0;
>     second_gaussian_waiting = 0;
>     rand48_seed = seed & INT64_LITERAL(0x00000000ffffffff);
>     rand48_seed = rand48_seed << 16;
>     rand48_seed |= RAND48_SEED & INT64_LITERAL(0x0000ffff);
>     rand48_mult = RAND48_MULT;
>     rand48_add = RAND48_ADD;
>     // print("INIT\n");
>   }
> 
>   HOST DEVICE inline void print(char* string)
>   {
> 	printf(string);
> 	printf("RAND48_SEED = %d, RAND48_MULT = %d, RAND48_ADD = %d, RAND48_MASK = %d\n", RAND48_SEED, RAND48_MULT, RAND48_ADD, RAND48_MASK);
> 	printf("rand48_seed = %d, rand48_mult = %d, rand48_add = %d\n", rand48_seed, rand48_mult, rand48_add);
>   }
> 
>   // advance generator by one (seed = seed * mult + add, to 48 bits)
>   HOST DEVICE inline void skip(void) {
>     rand48_seed = ( rand48_seed * rand48_mult + rand48_add ) & RAND48_MASK;
>   }
> 
>   // split into numStreams different steams and take stream iStream
>   void split(int iStream, int numStreams) {
>     int i;
> 
>     // make sure that numStreams is odd to ensure maximum period
>     numStreams |= 1;
> 
>     // iterate to get to the correct stream
>     for ( i = 0; i < iStream; ++i ) skip();
> 
>     // save seed and add so we can use skip() for our calculations
>     int64 save_seed = rand48_seed;
> 
>     // calculate c *= ( 1 + a + ... + a^(numStreams-1) )
>     rand48_seed = rand48_add;
>     for ( i = 1; i < numStreams; ++i ) skip();
>     int64 new_add = rand48_seed;
> 
>     // calculate a = a^numStreams
>     rand48_seed = rand48_mult;
>     rand48_add  = 0;
>     for ( i = 1; i < numStreams; ++i ) skip();
>     rand48_mult = rand48_seed;
> 
>     rand48_add  = new_add;
>     rand48_seed = save_seed;
> 
>     second_gaussian = 0;
>     second_gaussian_waiting = 0;
>     print("END SPLIT\n");
>   }
> 
>   // return a number uniformly distributed between 0 and 1
>   HOST DEVICE inline BigReal uniform(void) {
>     skip();
>     const float exp48 = ( 1.0 / (float)(INT64_LITERAL(1) << 48) );
>     return ( (float) rand48_seed * exp48 );
>   }
> 
>   long poisson(BigReal lambda) {
>     const BigReal l = exp(-lambda);
>     long k = 0;
>     BigReal p = uniform();
>     
>     while (p >= l) {
>       p *= uniform();
>       k = k + 1;
>     }
> 
>     return k;
>   }
> 
>   // return a number from a standard gaussian distribution
>   HOST DEVICE inline BigReal gaussian(void) {
>     BigReal fac, r, v1, v2;
> 
>     if (second_gaussian_waiting) {
>       second_gaussian_waiting = 0;
>       return second_gaussian;
>     } else {
>       r = 2.;                 // r >= 1.523e-8 ensures abs result < 6
>       while (r >=1. || r < 1.523e-8) { // make sure we are within unit circle
>         v1 = 2.0 * uniform() - 1.0;
>         v2 = 2.0 * uniform() - 1.0;
>         r = v1*v1 + v2*v2;
> 				// printf("r %f", r);
>       }
>       fac = sqrt(-2.0 * log(r)/r);
>       // now make the Box-Muller transformation to get two normally
>       // distributed random numbers. Save one and return the other.
>       second_gaussian_waiting = 1;
>       second_gaussian = v1 * fac;
>       return v2 * fac;
>     }
> 
>   }
> 
>   // return a vector of gaussian random numbers
>   HOST DEVICE inline Vector3 gaussian_vector(void) {
>     return Vector3( gaussian(), gaussian(), gaussian() );
>   }
> 
>   // return a random long
>   HOST DEVICE inline long integer(void) {
>     skip();
>     return ( ( rand48_seed >> 17 ) & INT64_LITERAL(0x000000007fffffff) );
>   }
> 
>   // randomly order an array of whatever
>   template <class Elem> void reorder(Elem *a, int n) {
>     for ( int i = 0; i < (n-1); ++i ) {
>       int ie = i + ( integer() % (n-i) );
>       if ( ie == i ) continue;
>       const Elem e = a[ie];
>       a[ie] = a[i];
>       a[i] = e;
444,502c189
<     HOST DEVICE ~RandomGPU() {
< 	LOGINFO("Destroying RandomGPU");
< 	assert( threadIdx.x + blockIdx.x == 0 );
< 	if (states != nullptr) delete [] states;
< 	states = nullptr;
<     }
< 
<     HOST static RandomGPU<num_states>* instantiate_on_GPU(RandomGPU<num_states> *dest = nullptr) {
< 	using RNG = RandomGPU<num_states>;
< 	RNG* rng = reinterpret_cast<RNG*>(new char[sizeof(RNG)]); // avoid calling constructor
< 	if (dest == nullptr) {
< 	    gpuErrchk(cudaMalloc((void**)&dest, sizeof(RNG)));
< 	}
< 	gpuErrchk(cudaMalloc((void**)&(rng->states), sizeof(state_t)*num_states));
< 	gpuErrchk(cudaMemcpy(dest, rng, sizeof(RNG),cudaMemcpyHostToDevice));
< 	rng->states = nullptr;
< 	delete rng;
< 	return dest;
<     }
< 	
<     HOST DEVICE inline float gaussian(state_t* state) {
< #ifdef __CUDA_ARCH__    
< 	return curand_normal( state );
< #else
< 	return 0.0f;
< #endif
<     };
<     
<     // Useful for getting the RNG state for the thread from global
<     // memory, putting it in local memory for use
<     HOST DEVICE inline state_t* get_gaussian_state() {
< #ifdef __CUDA_ARCH__    
< 	const size_t& i = threadIdx.x + blockDim.x*blockIdx.x;
< 	return (i < num_states && states != nullptr) ? &(states[i]) : nullptr;
< #else
< 	return (state_t*) nullptr;
< #endif
<     };
< 
<     HOST DEVICE inline void set_gaussian_state(state_t* state) {
< #ifdef __CUDA_ARCH__    
< 	const size_t& i = threadIdx.x + blockDim.x*blockIdx.x;
< 	if (i < num_states && state != nullptr) states[i] = *state;
< #endif
<     };
< 
<     HOST DEVICE inline Vector3 gaussian_vector( state_t* state) {
< 	return Vector3( gaussian(state), gaussian(state), gaussian(state) );
<     };
< 
<     template<size_t kernel_threads = 64> 
<     static void test(size_t num_vals = 4*num_states, unsigned long seed = 1, size_t offset = 0, RandomGPU<num_states>* rng = nullptr) {
< 	LOGINFO("Creating RandomGPU<{}>", num_states);
< 	using RNG = RandomGPU<num_states>;
< 	static_assert( (num_states >= kernel_threads) );
< 	static_assert( (num_states % kernel_threads) == 0 );
<     
< 	bool rng_was_null = (rng == nullptr);
< 	if (rng_was_null) rng = RNG::instantiate_on_GPU();
---
>   }
504,531d190
< 	float *buf_d, *buf;
< 
< 	LOGTRACE("cudaMalloc {}", num_vals);
< 	gpuErrchk(cudaMalloc((void**)&buf_d, num_vals*sizeof(float)));
< 	LOGTRACE("...launching kernel");
< 	RandomGPU__test_kernel<num_states><<<1+(num_states-1)/kernel_threads, kernel_threads>>>(rng, num_vals, buf_d, seed, offset);
< 	buf = new float[num_vals];
< 	gpuErrchk(cudaDeviceSynchronize());
< 	LOGTRACE("...copying data back");
<     
< 	gpuErrchk(cudaMemcpy(buf, buf_d, num_vals*sizeof(float),cudaMemcpyDeviceToHost));
< 	float sum = 0;
< 	float sum2 = 0;
< 	for (size_t i = 0; i < num_vals; ++i) {
< 	    // if (((i+1)%64) < 2) LOGINFO("...running test: buf[{}] == {}", i, buf[i]);
< 	    LOGINFO("...running test: buf[{}] == {}", i, buf[i]);
< 	    sum += buf[i];
< 	    sum2 += buf[i]*buf[i];
< 	}
< 	LOGINFO("RNG gaussian val0, val1, mean, std, count: {}, {}, {}, {}", buf[0], buf[1], sum/num_vals, sqrt((sum2/num_vals) - sum*sum/(num_vals*num_vals)), num_vals);
< 
< 	if (rng_was_null) cudaFree( rng );
< 	cudaFree( buf_d );
< 	delete[] buf;
<     };
< 
< private:
<     state_t* states;	// RNG states stored in global memory 
534,553c193
< template<size_t num_states = 128>
< __global__ void RandomGPU__test_kernel(RandomGPU<num_states>* rng, size_t num_vals, float* buf, unsigned long seed, size_t offset = 0) {
<     using RNG = RandomGPU<num_states>;
<     // printf("THREAD %d\n", threadIdx.x);
<     auto local_state = Random::get_gaussian_state<RNG>(rng);
<     for (size_t idx = threadIdx.x + blockIdx.x*blockDim.x; idx < num_states; idx += blockDim.x * gridDim.x) {
< 	assert(blockIdx.x == 0);
< 	// curand_init(unsigned long long seed,
< 	// 	    unsigned long long sequence,
< 	// 	    unsigned long long offset,
< 	// 	    curandStateXORWOW_t *state)
< 	curand_init(seed, idx, offset, local_state);
<     }
<     __syncthreads();
< 	
<     for (size_t idx = threadIdx.x + blockIdx.x*blockDim.x; idx < num_vals; idx += blockDim.x * gridDim.x) {
< 	buf[idx] = Random::gaussian<RNG>(rng, local_state);
<     }
<     Random::set_gaussian_state<RNG>(rng, local_state);
< };
---
> #endif  // RANDOM_H
555d194
< #endif
Only in /home/pinyili2/server10/arbd_stable/src: Reader.h
Only in /home/pinyili2/server10/arbd_stable/src: Reservoir.cu
Only in /home/pinyili2/server10/arbd_stable/src: Reservoir.h
Only in /home/pinyili2/server10/arbd_stable/src: Restraint.h
Only in /home/pinyili2/server10/arbd_stable/src: RigidBodyController.cu
Only in /home/pinyili2/server10/arbd_stable/src: RigidBodyController.h
Only in /home/pinyili2/server10/arbd_stable/src: RigidBody.cu
Only in /home/pinyili2/server10/arbd_stable/src: RigidBodyGrid.cu
Only in /home/pinyili2/server10/arbd_stable/src: RigidBodyGrid.h
Only in /home/pinyili2/server10/arbd_stable/src: RigidBody.h
Only in /home/pinyili2/server10/arbd_stable/src: RigidBodyType.cu
Only in /home/pinyili2/server10/arbd_stable/src: RigidBodyType.h
Only in /home/pinyili2/server10/arbd_stable/src: Scatter.cpp
Only in /home/pinyili2/server10/arbd_stable/src: Scatter.h
diff arbd.patches/src/SignalManager.cpp /home/pinyili2/server10/arbd_stable/src/SignalManager.cpp
2d1
< 
6c5
< #include "common.h"
---
> 
37,39d35
< #ifdef USE_LOGGER
<     spdlog::set_level(spdlog::level::trace);
< #endif
51,56c47
< void SignalManager::manage_segfault() {
< #ifdef USE_LOGGER
<     // Commented when moving to logger.h
<     // spdlog::set_level(spdlog::level::trace);
< #endif
< }
---
> void SignalManager::manage_segfault() {}
diff arbd.patches/src/SignalManager.h /home/pinyili2/server10/arbd_stable/src/SignalManager.h
9,56d8
< #ifdef USE_LOGGER
< 
< /* #define ARBD_LOG_ACTIVE_LEVEL 0 */
< /* #include "logger.h" */
< 
< //*
< #define FMT_HEADER_ONLY
< #include <spdlog/fmt/bundled/core.h>
< #include <spdlog/fmt/bundled/format.h>
< #include <spdlog/spdlog.h>
< 
< #ifndef SPDLOG_ACTIVE_LEVEL
< #define SPDLOG_ACTIVE_LEVEL SPDLOG_LEVEL_TRACE
< #endif
< 
< #define LOGTRACE(...) SPDLOG_TRACE(__VA_ARGS__)
< #define LOGDEBUG(...) SPDLOG_DEBUG(__VA_ARGS__)
< // #define DEBUG(...) spdlog::debug(__VA_ARGS__)
< #define LOGINFO(...) SPDLOG_INFO(__VA_ARGS__)
< #define LOGWARN(...) SPDLOG_WARN(__VA_ARGS__)
< #define LOGERROR(...) SPDLOG_ERROR(__VA_ARGS__)
< #define LOGCRITICAL(...) SPDLOG_CRITICAL(__VA_ARGS__)
< // spdlog::set_level(spdlog::level::trace);
< //*/
< 
< /*
< #define TRACE(...) ::arbd::log_trace(__VA_ARGS__)
< #define DEBUG(...) ::arbd::log_debug(__VA_ARGS__)
< // #define DEBUG(...) spdlog::debug(__VA_ARGS__)
< #define INFO(...) ::arbd::log_info(__VA_ARGS__)
< #define WARN(...) ::arbd::log_warn(__VA_ARGS__)
< #define ERROR(...) ::arbd::log_error(__VA_ARGS__)
< #define CRITICAL(...) ::arbd::log_critical(__VA_ARGS__)
< //*/
< 
< #else
< 
< // Disable logger macros
< // NOTE to developers: only use the macros below for logging, only in host code
< #define TRACE(...)
< #define DEBUG(...)
< #define INFO(...)
< #define WARN(...)
< #define ERROR(...)
< #define CRITICAL(...)
< 
< #endif
< 
73a26,27
> 
> 
Only in arbd.patches/src: SimManager.cu
Only in arbd.patches/src: SimManager.h
Only in arbd.patches/src: SimSystem.cpp
Only in arbd.patches/src: SimSystem.h
Only in /home/pinyili2/server10/arbd_stable/src: TabulatedAngle.cu
Only in /home/pinyili2/server10/arbd_stable/src: TabulatedAngle.h
Only in /home/pinyili2/server10/arbd_stable/src: TabulatedDihedral.cu
Only in /home/pinyili2/server10/arbd_stable/src: TabulatedDihedral.h
Only in /home/pinyili2/server10/arbd_stable/src: TabulatedMethods.cuh
Only in /home/pinyili2/server10/arbd_stable/src: TabulatedPotential.cu
Only in /home/pinyili2/server10/arbd_stable/src: TabulatedPotential.h
Only in /home/pinyili2/server10/arbd_stable/src: TAGS.sh
Only in arbd.patches/src: test
Only in arbd.patches/src: test.cpp
Only in arbd.patches/src: Tests
Only in arbd.patches/src: Tests.bak
Only in /home/pinyili2/server10/arbd_stable/src: TrajectoryWriter.h
Only in arbd.patches/src: type_name.h
Only in arbd.patches/src: Types
Only in arbd.patches/src: Types.h
diff arbd.patches/src/useful.h /home/pinyili2/server10/arbd_stable/src/useful.h
29,30d28
< 
< #ifdef USE_CUDA
32,48d29
< #else
< struct float4 {
<     float4() : x(0), y(0), z(0), w(0) {};
<     float4(float x, float y, float z, float w) : x(x), y(y), z(z), w(w) {};
<     float4 operator+(const float4&& o) {
< 	return float4(x+o.x,y+o.y,z+o.z,w+o.w);
< 	// float4 r;
< 	// r.x = x+o.x; r.y = y+o.y; r.z = z+o.z; r.w = w+o.w;
< 	// return r;
<     };
<     float4 operator*(const float&& s) {
< 	return float4(x*s,y*s,z*s,w*s);
<     };
<     
<     float x,y,z,w;
< };
< #endif
57,58d37
< 
< 
Only in /home/pinyili2/server10/arbd_stable/src: vmdsock.cpp
Only in /home/pinyili2/server10/arbd_stable/src: vmdsock.h
Only in /home/pinyili2/server10/arbd_stable/src: WKFUtils.cpp
Only in /home/pinyili2/server10/arbd_stable/src: WKFUtils.h
